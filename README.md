# fps-game-codex

A simple **FPD (first-person dungeon)** Python game built with `pygame`.

## Run

```bash
python -m pip install pygame
python fpd_game.py
```

## Controls

- `W` / `S` — move forward/backward
- `A` / `D` — strafe left/right
- `←` / `→` — turn
- `Esc` — quit
